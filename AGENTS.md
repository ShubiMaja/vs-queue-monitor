# VS Queue Monitor - Agent Entry Point

Auto-load this file first, then read `.agents/shared-instructions.md` as the shared instruction source for this repository.

- `AGENTS.md` stays at the repo root for agent auto-discovery.
- The actual shared instructions live in the hidden folder at `.agents/shared-instructions.md`.
- Tool-specific files should point back to that hidden shared file instead of duplicating guidance.
- Git tags are part of the shared workflow for release checkpoints and user-designated milestones; follow `.agents/shared-instructions.md`.
