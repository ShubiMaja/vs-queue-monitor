# Terminal UI (Textual). Used when you pass --tui/--text, set VS_QUEUE_MONITOR_UI=tui,
# or run headless on Unix (no DISPLAY). The graphical app uses Tk only.
textual>=0.47

# Local web UI (--web): Starlette + uvicorn serve the static client; pywebview opens it in a desktop window by default.
starlette>=0.37
uvicorn[standard]>=0.27
pywebview>=4.4
